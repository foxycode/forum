{block title}- [{$thread->subject}]{/block}

{block content}
    {if !$preview}
        <nav>
            <a n:href="Homepage:">Návrat do fóra</a> |
            <a n:href="Thread:reply $thread->thread_id">Vložit odpověd</a> |
            <a n:href="Thread: $thread->thread_id + 1">Novější</a> |
            <a n:href="Thread: $thread->thread_id - 1">Starší</a>
        </nav>
    {/if}

    {foreach $messages as $message}
        {include '../common/message.latte' message => $message, iterator => $iterator}
    {/foreach}

    <hr>

    {if !$preview}
        <nav>
            <a n:href="Homepage:">Návrat do fóra</a> |
            <a n:href="Thread:reply $thread->thread_id">Vložit odpověd</a> |
            <a n:href="Thread: $thread->thread_id + 1">Novější</a> |
            <a n:href="Thread: $thread->thread_id - 1">Starší</a>
        </nav>
    {else}
        Toto je náhled zprávy, do které vkládáte odpověd.
    {/if}
{/block}
